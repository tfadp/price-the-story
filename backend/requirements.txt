# Web framework
fastapi==0.115.5
uvicorn[standard]==0.32.1
sse-starlette==2.1.3
python-multipart==0.0.12

# LangGraph + LangChain (orchestration + LLM)
langgraph==0.2.56
langchain-anthropic==0.3.3
langchain-core==0.3.25

# Data sources
yfinance==0.2.50
httpx==0.28.0

# Pydantic + config
pydantic==2.10.3
pydantic-settings==2.6.1
python-dotenv==1.0.1

# Cache (Redis — optional in Phase 1, falls back to no-op)
redis==5.2.0

# Testing
pytest==8.3.3
pytest-asyncio==0.24.0
httpx==0.28.0

# Linting (run: ruff check backend/)
ruff==0.8.0
